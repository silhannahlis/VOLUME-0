#include <stdio.h>

int main() {
    int x,sum=0;
    printf("sayı gir: ");scanf("%d",&x);
    for(int i=0; x!=0;i++){
        sum+=x%10;
        printf("%d\n",x%10);
        x= x/10;
    }
printf("%d ",sum);
}

#include <stdio.h>

int main() {
   int i,j,l,k,n,m=0;
   for(i=1;i<=9;i++){
       for(j=1;j<=9;j++){
           for(k=1;k<=9;k++){
               n=(i*100)+(j*10)+k; //printf("%d\t",n);
               if( (n%i==0) && (n%j==0) && (n%k==0) ){   //m++; printf("\n%d\n ",m);
                   printf("%d ",n);   }
           }
       }
   }

    return 0;
}
